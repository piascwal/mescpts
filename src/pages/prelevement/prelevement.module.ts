import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { PrelevementPage } from './prelevement';

@NgModule({
  declarations: [
    PrelevementPage,
  ],
  imports: [
    IonicPageModule.forChild(PrelevementPage),
  ],
})
export class PrelevementPageModule {}
